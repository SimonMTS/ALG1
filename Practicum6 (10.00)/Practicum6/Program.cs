using Alg1.Practica.Practicum6;
using Alg1.Practica.Utils.Models;
using System;

namespace Practicum6
{
    class MainClass
    {
        public static void Main(string[] args)
        {
            //NawPriorityQueue queue = new NawPriorityQueue();

            //queue.Enqueue(9, new NAW("0", "0", "9"));
            //queue.Enqueue(9, new NAW("0", "0", "7"));
            ////queue.Enqueue(9, new NAW("0", "0", "5"));
            ////queue.Enqueue(9, new NAW("0", "0", "8"));
            ////queue.Enqueue(9, new NAW("0", "0", "6"));

            //queue.Show();

            //Console.WriteLine(queue.Dequeue());
            //Console.WriteLine(queue.Dequeue());
            ////Console.WriteLine(queue.Dequeue());
            ////Console.WriteLine(queue.Dequeue());
            ////Console.WriteLine(queue.Dequeue());
            ////Console.WriteLine(queue.Dequeue());

            //Console.WriteLine(queue.Count());

            //Console.ReadKey();
        }
    }
}
